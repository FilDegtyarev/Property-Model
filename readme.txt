Я понимаю, что я сделал два френда, но я не вижу пока что другого выбора. Builder должен быть другом к PM, чтобы только он вызывал ее конструкторы.
Но так как есть еще PMImpl - Builder должен быть другом к ней, чтобы пользователь через operator -> не получил доступ к техническим методам PMImpl.
(В том смысле, что PM-> возвращает объект типа PmImpl)

Я немного изменил названия методов у алгоритма. 
Я хочу, чтобы сейчас все переменные, методы и constraint модели были адресуемыми, так что после того, как модель создана билдером в нее НЕ вносятся изменния в смысле "добавления переменной / ограничения" и удалений.
Вместо этого я могу попросить модель "включить" ограничение - то есть добавить его в Constraint graph. За это отвечает поле "Enable" у Variable.
Я провожу биндинги к переменным только тогда, когда я уже не добавляю/удаляя новые - так что реаллокаций в векторе не происходит.
Аналогично с меотдами.

В файле test.cpp я приложил несколько простых примеров пользования моделью, без алгоритма. Сейчас, я бы очень хотел услышать какие-то замечания по архитектуре проекта.
Заранее спасибо!